# Data is fetched reproducibly from OpenML (id 42175) by src/load_data.py.
# The raw CSV is ~148 MB, over GitHub's 100 MB limit, so it is never committed.
data/*.csv

# Training artifacts / logs (regenerated by the scripts and notebook)
artifacts/
logs/
*.keras
*.h5

# Local-only report generator (kept out of the submitted repo)
make_report.py

# Python / Jupyter
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
